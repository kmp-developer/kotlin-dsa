package naser09.github.io.dsa.data_structures.c_complex

/**
 * A 2-3 tree is a tree data structure, where every node with children (internal node) has either two children (a 2-node) and one data element or three children (a 3-node) and two data elements.
 */
